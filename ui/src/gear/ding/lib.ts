import _ from 'lodash';
import {
    Flag,
    // DingAction,
} from './types';

// function dingAction(action: DingAction) {
//   return {
//     app: 'ding',
//     mark: 'ding-action',
//     json: action,
//   };
// }
//  scry bundles and  subscribe for all updates 
export function useBundles(flag?: Flag) {
  if(flag){
    return;
  }
  return ;
}